{
  "name": "foodflux-root",
  "version": "1.0.0",
  "description": "Root package.json for one-click deployment on Render.com",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js"
  }
}
